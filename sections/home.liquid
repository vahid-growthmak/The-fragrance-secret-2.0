{%- comment -%}
  Homepage, rebuilt to the approved 13-section reference layout.

  Order: hero, trust bar, brands, best sellers, new arrivals, shop by category,
  shop by scent family, shop by moment (lifestyle + climate), guides, instagram,
  FAQ, why choose. The 10% welcome band is section 13 of the reference and is
  already rendered sitewide above the footer by customer-login-cta, so it is not
  duplicated here.

  Deliberately omitted from the reference: the review score, the "10,284 verified
  reviews" count, the named testimonials and the hero rating line, all of which
  are fabricated.

  Every card row resolves collections by handle and skips handles with no
  matching collection, so a tile can never link to a page that does not exist.
{%- endcomment -%}

{%- liquid
  assign house_handles = section.settings.house_handles | split: ','
  assign popular_handles = section.settings.popular_handles | split: ','
-%}

<!-- 1 · HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-content">
    {%- if section.settings.hero_eyebrow != blank -%}
      <p class="hero-pretitle">{{ section.settings.hero_eyebrow }}</p>
    {%- endif -%}
    {%- comment -%}
      Two explicit lines rather than one string with a <br>: each is its own
      block, so the heading is always exactly two lines and never breaks into a
      third when the longer line outgrows the column.
    {%- endcomment -%}
    <h1 class="hero-h">
      <span class="hero-h-line">{{ section.settings.hero_line_1 }}</span>
      {%- if section.settings.hero_line_2 != blank -%}
        <span class="hero-h-line"><em>{{ section.settings.hero_line_2 }}</em></span>
      {%- endif -%}
    </h1>
    {%- if section.settings.hero_tagline != blank or section.settings.hero_tagline_2 != blank -%}
      <p class="hero-tagline">
        {%- if section.settings.hero_tagline != blank -%}
          <span class="hero-tagline-line">{{ section.settings.hero_tagline }}</span>
        {%- endif -%}
        {%- if section.settings.hero_tagline_2 != blank -%}
          <span class="hero-tagline-line">{{ section.settings.hero_tagline_2 }}</span>
        {%- endif -%}
      </p>
    {%- endif -%}
    <p class="hero-sub">{{ section.settings.hero_sub }}</p>
    <div class="hero-ctas">
      {%- comment -%}
        Fallbacks matter here: a url setting cannot carry an arbitrary schema
        default, so an unset one renders href="" — a link that silently reloads
        the page instead of going anywhere.
      {%- endcomment -%}
      <a class="btn-g" href="{{ section.settings.hero_cta_url | default: routes.all_products_collection_url }}">
        <span class="mi" aria-hidden="true">shopping_bag</span>{{ section.settings.hero_cta_label }}
      </a>
      <a class="hero-textlink" href="{{ section.settings.hero_link_url | default: '/collections/best-sellers' }}">{{ section.settings.hero_link_label }}
        <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
  </div>
  <div class="hero-img-wrap">
    {%- comment -%}
      Full-bleed artwork. Uploaded images are served through image_url so Shopify
      generates the responsive sizes; the packaged asset is the fallback.
    {%- endcomment -%}
    {%- assign hero_image = section.settings.hero_image -%}
    {%- if hero_image != blank -%}
      <img class="hero-img"
        src="{{ hero_image | image_url: width: 2880 }}"
        srcset="{{ hero_image | image_url: width: 900 }} 900w,
                {{ hero_image | image_url: width: 1400 }} 1400w,
                {{ hero_image | image_url: width: 1900 }} 1900w,
                {{ hero_image | image_url: width: 2400 }} 2400w,
                {{ hero_image | image_url: width: 2880 }} 2880w"
        sizes="100vw"
        width="{{ hero_image.width }}" height="{{ hero_image.height }}"
        style="object-position: {{ section.settings.hero_focus }}"
        alt="{{ hero_image.alt | default: 'Luxury perfume' | escape }}"
        fetchpriority="high" onload="this.classList.add('loaded')" />
    {%- else -%}
      {%- assign hero_asset = section.settings.hero_image_asset | default: 'home-hero.webp' -%}
      <img class="hero-img" src="{{ hero_asset | asset_url }}"
        {%- if section.settings.hero_image_asset_sm != blank %}
        srcset="{{ section.settings.hero_image_asset_sm | asset_url }} 900w, {{ hero_asset | asset_url }} 1672w"
        sizes="100vw"
        {%- endif %}
        style="object-position: {{ section.settings.hero_focus }}"
        alt="{{ section.settings.hero_image_alt | escape }}"
        fetchpriority="high" onload="this.classList.add('loaded')" />
    {%- endif -%}
  </div>
</section>

<!-- 2 · PERSISTENT TRUST BAR -->
<div class="trust-strip">
  <div class="trust-inner">
    <div class="ts-item"><span class="mi" aria-hidden="true">verified_user</span>
      <div><strong>100% Authentic</strong> — Verified sourcing</div>
    </div>
    <div class="ts-item"><span class="mi" aria-hidden="true">local_shipping</span>
      <div><strong>Free Delivery</strong> above AED 150</div>
    </div>
    <div class="ts-item"><span class="mi" aria-hidden="true">payments</span>
      <div><strong>Cash on Delivery</strong> available UAE-wide</div>
    </div>
    <div class="ts-item"><span class="mi" aria-hidden="true">chat</span>
      <div><strong>WhatsApp Support</strong> — daily 10am–7pm</div>
    </div>
  </div>
</div>

<!-- 3 · FEATURED BRANDS -->
<section class="brands" id="brands">
  <div class="brands-inner">
    <div class="best-head fade-up">
      <div>
        <span class="label">{{ section.settings.brands_eyebrow }}</span>
        <h2 class="sec-title" style="font-size:1.9rem">{{ section.settings.brands_heading }}</h2>
        {%- if section.settings.brands_sub != blank -%}<p class="sec-sub">{{ section.settings.brands_sub }}</p>{%- endif -%}
      </div>
      <a href="/pages/brands" class="see-all">All Brands A–Z <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>

    {%- comment -%} Our own houses, elevated above the resale A-Z. {%- endcomment -%}
    <div class="ourbrands">
      {%- for raw_handle in house_handles -%}
        {%- assign house_handle = raw_handle | strip -%}
        {%- assign house = collections[house_handle] -%}
        {%- if house.handle == blank -%}{%- continue -%}{%- endif -%}
        {%- assign house_name = house.title | remove: ' Perfumes' -%}
        <a href="{{ house.url }}" class="ob-card fade-up">
          <span class="ob-tag">Our Brand</span>
          <span class="ob-name">{{ house_name }}</span>
          <span class="ob-sub">{{ house.description | strip_html | truncatewords: 6 }}</span>
          <span class="ob-go">Shop the house <span class="mi" aria-hidden="true">arrow_forward</span></span>
        </a>
      {%- endfor -%}
    </div>

    {%- comment -%} Most-loved houses, then the full A-Z on the brands hub. {%- endcomment -%}
    <div class="azbrands-head fade-up">
      <span class="label" style="margin:0">Most Loved Houses</span>
      <span class="az-note">Verified Resale</span>
    </div>
    <div class="brand-cloud brand-cloud-row fade-up">
      {%- for raw_handle in popular_handles -%}
        {%- assign popular_handle = raw_handle | strip -%}
        {%- assign brand = collections[popular_handle] -%}
        {%- if brand.handle == blank -%}{%- continue -%}{%- endif -%}
        <a class="brand-chip" href="{{ brand.url }}">
          {{ brand.title | remove: ' Perfumes' }}
          {%- if brand.products_count > 0 -%}<span class="brand-chip-n">{{ brand.products_count }}</span>{%- endif -%}
        </a>
      {%- endfor -%}
      <a class="brand-chip brand-chip-all" href="/pages/brands">View all brands
        <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
  </div>
</section>

<!-- 4 · BEST SELLERS -->
<section class="best" id="best">
  <div class="best-inner">
    <div class="best-head fade-up">
      <div>
        <span class="label">{{ section.settings.best_eyebrow }}</span>
        <h2 class="sec-title">{{ section.settings.best_heading }}</h2>
        {%- if section.settings.best_sub != blank -%}<p class="sec-sub">{{ section.settings.best_sub }}</p>{%- endif -%}
      </div>
      <a href="/collections/best-sellers" class="see-all">View All <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
    {%- assign bestsellers = collections['best-sellers'] -%}
    {%- if bestsellers.products.size == 0 -%}{%- assign bestsellers = collections['featured'] -%}{%- endif -%}
    {%- if bestsellers.products.size > 0 -%}
      <div class="products-grid">
        {%- for product in bestsellers.products limit: 4 -%}{% render 'product-card', product: product %}{%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

<!-- 5 · NEW ARRIVALS -->
<section class="best" id="new">
  <div class="best-inner">
    <div class="best-head fade-up">
      <div>
        <span class="label">{{ section.settings.new_eyebrow }}</span>
        <h2 class="sec-title">{{ section.settings.new_heading }}</h2>
        {%- if section.settings.new_sub != blank -%}<p class="sec-sub">{{ section.settings.new_sub }}</p>{%- endif -%}
      </div>
      <a href="/collections/new-arrivals" class="see-all">View All <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
    {%- if collections['new-arrivals'].products.size > 0 -%}
      <div class="products-grid">
        {%- for product in collections['new-arrivals'].products limit: 4 -%}{% render 'product-card', product: product %}{%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

<!-- 6 · SHOP BY CATEGORY -->
<section class="gender" id="category">
  <div class="gender-inner">
    <div class="best-head fade-up">
      <div>
        <span class="label">{{ section.settings.category_eyebrow }}</span>
        <h2 class="sec-title">{{ section.settings.category_heading }}</h2>
        {%- if section.settings.category_sub != blank -%}<p class="sec-sub">{{ section.settings.category_sub }}</p>{%- endif -%}
      </div>
      <a href="/collections" class="see-all">Explore All Categories <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
    {%- comment -%}
      Landscape image cards: title plus one descriptor line, and an arrow on
      hover. The descriptor is per-card copy where the collection needs one
      ("Oud, musk & attar"), and falls back to the live product count.
      An uploaded image wins, then the collection image, then its first product.
    {%- endcomment -%}
    <div class="cat-grid">
      {%- for block in section.blocks -%}
        {%- if block.type != 'category' -%}{%- continue -%}{%- endif -%}
        {%- assign category = collections[block.settings.handle] -%}
        {%- if category.handle == blank -%}{%- continue -%}{%- endif -%}
        {%- assign cat_asset = block.settings.image_asset -%}
        {%- assign cat_image = block.settings.image | default: category.image | default: category.products.first.featured_image -%}
        {%- assign cat_title = block.settings.title | default: category.title -%}
        {%- assign cat_sizes = '(max-width: 600px) 92vw, (max-width: 900px) 46vw, 30vw' -%}
        <a class="cat-card fade-up" href="{{ category.url }}" {{ block.shopify_attributes }}>
          {%- if cat_asset != blank -%}
            <img class="cat-img" src="{{ cat_asset | asset_url }}"
                 {%- if block.settings.image_asset_sm != blank %}
                 srcset="{{ block.settings.image_asset_sm | asset_url }} 600w, {{ cat_asset | asset_url }} 1200w"
                 sizes="{{ cat_sizes }}"
                 {%- endif %}
                 alt="{{ cat_title | escape }}" loading="lazy" width="1200" height="750" />
          {%- elsif cat_image -%}
            <img class="cat-img" src="{{ cat_image | image_url: width: 900 }}"
                 srcset="{{ cat_image | image_url: width: 450 }} 450w,
                         {{ cat_image | image_url: width: 900 }} 900w,
                         {{ cat_image | image_url: width: 1200 }} 1200w"
                 sizes="{{ cat_sizes }}"
                 alt="{{ cat_title | escape }}" loading="lazy" width="900" height="563" />
          {%- endif -%}
          <span class="cat-arrow" aria-hidden="true"><span class="mi">arrow_forward</span></span>
          <div class="cat-ct">
            <h3>{{ cat_title }}</h3>
            {%- if block.settings.sub != blank -%}
              <span>{{ block.settings.sub }}</span>
            {%- elsif category.products_count > 0 -%}
              <span>{{ category.products_count }} fragrance{% if category.products_count != 1 %}s{% endif %}</span>
            {%- endif -%}
          </div>
        </a>
      {%- endfor -%}
    </div>
  </div>
</section>

<!-- 7 · SHOP BY SCENT FAMILY -->
<section class="scent-section" id="scent-family">
  <div class="container">
    <div class="sec-head center fade-up">
      <span class="label">{{ section.settings.scent_eyebrow }}</span>
      <h2 class="sec-title">{{ section.settings.scent_heading }}</h2>
      {%- if section.settings.scent_sub != blank -%}<p class="sec-sub">{{ section.settings.scent_sub }}</p>{%- endif -%}
    </div>
    <div class="scent-grid">
      {%- for block in section.blocks -%}
        {%- if block.type != 'scent' -%}{%- continue -%}{%- endif -%}
        {%- assign scent = collections[block.settings.handle] -%}
        {%- if scent.handle == blank -%}{%- continue -%}{%- endif -%}
        {%- assign scent_asset = block.settings.image_asset -%}
        {%- assign scent_image = block.settings.image | default: scent.image -%}
        <a class="scent-card fade-up{% if scent_asset != blank or scent_image %} has-bg{% endif %}"
           href="{{ scent.url }}" {{ block.shopify_attributes }}>
          {%- if scent_asset != blank -%}
            <img class="scent-bg" src="{{ scent_asset | asset_url }}"
                 {%- if block.settings.image_asset_sm != blank %}
                 srcset="{{ block.settings.image_asset_sm | asset_url }} 350w, {{ scent_asset | asset_url }} 700w"
                 sizes="(max-width: 600px) 46vw, 18vw"
                 {%- endif %}
                 alt="" loading="lazy" width="700" height="700" />
          {%- elsif scent_image -%}
            <img class="scent-bg" src="{{ scent_image | image_url: width: 600 }}"
                 srcset="{{ scent_image | image_url: width: 300 }} 300w,
                         {{ scent_image | image_url: width: 600 }} 600w"
                 sizes="(max-width: 600px) 46vw, 18vw"
                 alt="" loading="lazy" width="600" height="600" />
          {%- endif -%}
          <span class="scent-ic"><span class="mi" aria-hidden="true">{{ block.settings.icon }}</span></span>
          <h3>{{ block.settings.title | default: scent.title }}</h3>
          <p>{{ block.settings.sub }}</p>
        </a>
      {%- endfor -%}
    </div>
    {%- if section.settings.scent_all_url != blank -%}
      <div class="scent-cta fade-up">
        <a class="see-all" href="{{ section.settings.scent_all_url }}">{{ section.settings.scent_all_label }}
          <span class="mi" aria-hidden="true">arrow_forward</span></a>
      </div>
    {%- endif -%}
  </div>
</section>

<!-- 8 · SHOP BY MOMENT -->
<section class="moment-section" id="moment">
  <div class="container">
    <div class="sec-head center fade-up">
      <span class="label">{{ section.settings.moment_eyebrow }}</span>
      <h2 class="sec-title" style="color:var(--white)">{{ section.settings.moment_heading }}</h2>
      {%- if section.settings.moment_sub != blank -%}<p class="sec-sub">{{ section.settings.moment_sub }}</p>{%- endif -%}
    </div>
    <div class="moment-wrap">
      {%- comment -%} Two columns: lifestyle and climate, six tiles each. {%- endcomment -%}
      {%- assign moment_cols = 'lifestyle,climate' | split: ',' -%}
      {%- for col in moment_cols -%}
        {%- if col == 'lifestyle' -%}
          {%- assign col_title = 'By Lifestyle' -%}
          {%- assign col_lead = 'Dress your scent for the occasion.' -%}
          {%- assign col_all_label = 'All lifestyles' -%}
          {%- assign col_all_url = '/pages/shop-by-lifestyle' -%}
        {%- else -%}
          {%- assign col_title = 'By Climate' -%}
          {%- assign col_lead = 'Built for UAE heat, and beyond.' -%}
          {%- assign col_all_label = 'All climates' -%}
          {%- assign col_all_url = '/pages/shop-by-weather' -%}
        {%- endif -%}
        <div class="moment-col">
          <h3 class="moment-title">{{ col_title }}</h3>
          <p class="moment-lead">{{ col_lead }}</p>
          <div class="moment-tiles">
            {%- for block in section.blocks -%}
              {%- if block.type != 'moment' or block.settings.column != col -%}{%- continue -%}{%- endif -%}
              {%- assign moment = collections[block.settings.handle] -%}
              {%- if moment.handle == blank -%}{%- continue -%}{%- endif -%}
              {%- assign moment_label = block.settings.title | default: moment.title | remove: ' Perfumes' -%}
              {%- assign moment_asset = block.settings.image_asset -%}
              {%- assign moment_image = block.settings.image | default: moment.image | default: moment.products.first.featured_image -%}
              <a class="moment-tile" href="{{ moment.url }}" {{ block.shopify_attributes }}>
                {%- if moment_asset != blank -%}
                  <img src="{{ moment_asset | asset_url }}"
                       {%- if block.settings.image_asset_sm != blank %}
                       srcset="{{ block.settings.image_asset_sm | asset_url }} 350w, {{ moment_asset | asset_url }} 700w"
                       sizes="(max-width: 900px) 30vw, 14vw"
                       {%- endif %}
                       alt="{{ moment_label | escape }}" loading="lazy" width="700" height="700">
                {%- elsif moment_image -%}
                  <img src="{{ moment_image | image_url: width: 700 }}"
                       srcset="{{ moment_image | image_url: width: 350 }} 350w,
                               {{ moment_image | image_url: width: 700 }} 700w"
                       sizes="(max-width: 900px) 30vw, 14vw"
                       alt="{{ moment_label | escape }}" loading="lazy">
                {%- endif -%}
                <span>{{ moment_label }}</span>
              </a>
            {%- endfor -%}
          </div>
          <a class="see-all moment-all" href="{{ col_all_url }}">{{ col_all_label }}
            <span class="mi" aria-hidden="true">arrow_forward</span></a>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

<!-- 9 · GUIDES -->
<section class="blog" id="blog">
  <div class="blog-inner">
    <div class="blog-head fade-up">
      <div>
        <span class="label">{{ section.settings.guides_eyebrow }}</span>
        <h2 class="sec-title">{{ section.settings.guides_heading }}</h2>
        {%- if section.settings.guides_sub != blank -%}<p class="sec-sub">{{ section.settings.guides_sub }}</p>{%- endif -%}
      </div>
      <a href="/blogs/journal" class="see-all">All Articles <span class="mi" aria-hidden="true">arrow_forward</span></a>
    </div>
    {%- if blogs['journal'].articles.size > 0 -%}
      <div class="blog-grid">
        {%- for article in blogs['journal'].articles limit: 3 -%}
          <a class="bl-card" href="{{ article.url }}">
            <div class="bl-img-wrap">{% if article.image %}<img class="bl-img" src="{{ article.image | image_url: width: 700 }}" alt="{{ article.title | escape }}" loading="lazy">{% endif %}</div>
            {% if article.tags.size > 0 %}<div class="bl-cat">{{ article.tags.first }}</div>{% endif %}
            <div class="bl-title">{{ article.title }}</div>
            <div class="bl-excerpt">{{ article.excerpt_or_content | strip_html | truncatewords: 24 }}</div>
            <div class="bl-meta">{{ article.published_at | date: '%B %-d, %Y' }}</div>
          </a>
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</section>

<!-- 10 · INSTAGRAM -->
{%- comment -%}
  The feed itself comes from an Instagram app block, which holds and refreshes the
  Meta access token — a theme cannot do that safely, since anything it ships is
  readable in the browser and the token expires every 60 days.

  In the theme editor, add the app's block to this Home section and drag it here.
  The section renders only the heading until one is present, so the page never
  shows placeholder tiles pretending to be customer posts.
{%- endcomment -%}
{%- assign app_blocks = section.blocks | where: 'type', '@app' -%}
{%- if app_blocks.size > 0 or request.design_mode -%}
  <section class="ugc">
    <div class="ugc-inner">
      <div class="ugc-head fade-up">
        <span class="label">{{ section.settings.ugc_eyebrow }}</span>
        <h2 class="sec-title">{{ section.settings.ugc_heading }}</h2>
        {%- if section.settings.ugc_sub != blank -%}
          <p class="sec-sub center" style="margin:8px auto 0">{{ section.settings.ugc_sub }}</p>
        {%- endif -%}
      </div>
      {%- for block in app_blocks -%}
        <div class="ugc-app" {{ block.shopify_attributes }}>{% render block %}</div>
      {%- endfor -%}
      {%- if app_blocks.size == 0 and request.design_mode -%}
        <p class="ugc-empty">No Instagram feed connected yet. Use <strong>Add block</strong> on this
          Home section and pick your Instagram app's block, then drag it here. This
          message is only visible in the theme editor.</p>
      {%- endif -%}
    </div>
  </section>
{%- endif -%}

<!-- 11 · FAQ -->
<section class="faq" id="faq">
  <div class="faq-inner">
    <div class="faq-head fade-up">
      <span class="label">Your Questions, Answered</span>
      <h2 class="sec-title">Frequently Asked <em>Questions</em></h2>
    </div>
    <div class="faq-list fade-up">
      {%- for block in section.blocks -%}
        {%- if block.type != 'faq' -%}{%- continue -%}{%- endif -%}
        <details class="faq-item" {{ block.shopify_attributes }}>
          <summary class="faq-q">{{ block.settings.question }}<span class="mi" aria-hidden="true">add</span></summary>
          <div class="faq-a">{{ block.settings.answer }}</div>
        </details>
      {%- endfor -%}
    </div>
  </div>
</section>

<!-- 12 · WHY CHOOSE THE FRAGRANCE SECRETS -->
<section class="section why-section">
  <div class="container">
    <div class="sec-head center fade-up">
      <span class="label">Why Shop With Us</span>
      <h2 class="sec-title">Why Choose <em>The Fragrance Secrets</em></h2>
      {%- if section.settings.why_sub != blank -%}
        <p class="sec-sub">{{ section.settings.why_sub }}</p>
      {%- endif -%}
    </div>
    {%- comment -%}
      Column count follows the number of cards, so they always sit on one row
      whether there are three, four or five of them.
    {%- endcomment -%}
    {%- assign why_count = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'why' -%}{%- assign why_count = why_count | plus: 1 -%}{%- endif -%}
    {%- endfor -%}
    <div class="why-grid why-grid-auto" style="--why-cols: {{ why_count | default: 4 }}">
      {%- for block in section.blocks -%}
        {%- if block.type != 'why' -%}{%- continue -%}{%- endif -%}
        <div class="why-card fade-up" {{ block.shopify_attributes }}>
          <span class="mi" aria-hidden="true">{{ block.settings.icon }}</span>
          <h3>{{ block.settings.title }}</h3>
          {%- if block.settings.body != blank -%}<p>{{ block.settings.body }}</p>{%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{%- comment -%}
  Section 13 of the reference, the 10% welcome band, is rendered sitewide above
  the footer by sections/customer-login-cta.liquid, so it is not repeated here.
{%- endcomment -%}

{% schema %}
{
  "name": "Home",
  "settings": [
    {
      "type": "header",
      "content": "1 · Hero"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Background image",
      "info": "Full width behind the copy. Upload 2880 × 1620 px (16:9). Keep the subject right of centre — the left side carries a dark scrim for the text. Falls back to hero-roses.jpg."
    },
    {
      "type": "text",
      "id": "hero_image_asset",
      "label": "Background image (theme asset filename)",
      "info": "Used when no image is uploaded above. Defaults to home-hero.webp.",
      "default": "home-hero.webp"
    },
    {
      "type": "text",
      "id": "hero_image_asset_sm",
      "label": "Small background image (theme asset)",
      "info": "Optional narrower variant served to phones.",
      "default": "home-hero-sm.webp"
    },
    {
      "type": "text",
      "id": "hero_image_alt",
      "label": "Image alt text",
      "default": "Luxury perfume bottles at The Fragrance Secrets"
    },
    {
      "type": "select",
      "id": "hero_focus",
      "label": "Image focus",
      "info": "Which part of the image to keep when it is cropped.",
      "options": [
        {
          "value": "center center",
          "label": "Centre"
        },
        {
          "value": "right center",
          "label": "Right"
        },
        {
          "value": "left center",
          "label": "Left"
        },
        {
          "value": "center top",
          "label": "Top"
        },
        {
          "value": "center bottom",
          "label": "Bottom"
        }
      ],
      "default": "right center"
    },
    {
      "type": "text",
      "id": "hero_eyebrow",
      "label": "Eyebrow",
      "info": "Left empty so it does not repeat the H1. The Home Page document provides no eyebrow."
    },
    {
      "type": "text",
      "id": "hero_line_1",
      "label": "Heading line 1",
      "default": "Buy Perfume Online UAE"
    },
    {
      "type": "text",
      "id": "hero_line_2",
      "label": "Heading line 2",
      "info": "Rendered italic in gold on its own line. Leave blank for a one-line heading. Left empty deliberately: the Home Page document provides no value for this."
    },
    {
      "type": "textarea",
      "id": "hero_sub",
      "label": "Subheading",
      "default": "Explore authentic designer, Arabic and luxury perfumes for every style and occasion at The Fragrance Secrets. Enjoy delivery across the UAE with international shipping to the USA and Canada."
    },
    {
      "type": "text",
      "id": "hero_cta_label",
      "label": "Button label",
      "default": "Shop Perfumes"
    },
    {
      "type": "url",
      "id": "hero_cta_url",
      "label": "Button link"
    },
    {
      "type": "text",
      "id": "hero_link_label",
      "label": "Text link label",
      "default": "or explore best sellers"
    },
    {
      "type": "url",
      "id": "hero_link_url",
      "label": "Text link"
    },
    {
      "type": "header",
      "content": "3 · Brands"
    },
    {
      "type": "html",
      "id": "brands_heading",
      "label": "Heading",
      "default": "Explore Our Brands & <em>Global Fragrance Houses</em>"
    },
    {
      "type": "textarea",
      "id": "house_handles",
      "label": "Our own houses (comma separated handles)",
      "info": "Handles with no matching collection are skipped, so a card can never link to a page that does not exist.",
      "default": "fragrance-secrets, secret-scents, paris-collection"
    },
    {
      "type": "textarea",
      "id": "popular_handles",
      "label": "Most loved houses (comma separated handles)",
      "default": "lattafa, rasasi, armaf, swiss-arabian, ahmed-al-maghribi, al-haramain, amouage, ibrahim-al-qurashi"
    },
    {
      "type": "header",
      "content": "7 · Shop by scent family"
    },
    {
      "type": "text",
      "id": "scent_all_label",
      "label": "CTA label",
      "default": "All Scent Families"
    },
    {
      "type": "url",
      "id": "scent_all_url",
      "label": "CTA link",
      "info": "The scent family hub. Leave blank to hide the button."
    },
    {
      "type": "header",
      "content": "10 · Instagram"
    },
    {
      "type": "text",
      "id": "ugc_eyebrow",
      "label": "Eyebrow",
      "default": "Insta Shop"
    },
    {
      "type": "html",
      "id": "ugc_heading",
      "label": "Heading",
      "default": "Real People. <em>Real Scents.</em>"
    },
    {
      "type": "text",
      "id": "ugc_sub",
      "label": "Subheading",
      "default": "Tag us @thefragrancesecrets to be featured"
    },
    {
      "type": "header",
      "content": "12 · Why choose"
    },
    {
      "type": "textarea",
      "id": "why_sub",
      "label": "Intro",
      "info": "The reference marks this section as a copy placeholder pending the approved homepage document. The cards below default to the wording already signed off in the collection documents."
    },
    {
      "type": "text",
      "id": "brands_eyebrow",
      "label": "Brands Eyebrow",
      "default": "Shop By Brand"
    },
    {
      "type": "textarea",
      "id": "brands_sub",
      "label": "Brands Intro",
      "default": "Browse authentic perfume brands, including our exclusive collections and globally trusted fragrance houses loved across the UAE."
    },
    {
      "type": "text",
      "id": "best_eyebrow",
      "label": "Best Eyebrow",
      "default": "Best Sellers"
    },
    {
      "type": "html",
      "id": "best_heading",
      "label": "Best Heading",
      "default": "Best Selling Perfumes in UAE <em>Loved by Our Customers</em>"
    },
    {
      "type": "textarea",
      "id": "best_sub",
      "label": "Best Intro",
      "default": "Explore our bestselling perfumes, carefully selected for lasting impressions, premium quality and unforgettable fragrance experiences."
    },
    {
      "type": "text",
      "id": "new_eyebrow",
      "label": "New Eyebrow",
      "default": "New Arrivals"
    },
    {
      "type": "html",
      "id": "new_heading",
      "label": "New Heading",
      "default": "Fresh Fragrances, <em>Just Arrived for You</em>"
    },
    {
      "type": "textarea",
      "id": "new_sub",
      "label": "New Intro",
      "default": "Shop our latest authentic designer, Arabic and luxury perfumes, carefully selected for every style and occasion."
    },
    {
      "type": "text",
      "id": "category_eyebrow",
      "label": "Category Eyebrow",
      "default": "Shop By Category"
    },
    {
      "type": "html",
      "id": "category_heading",
      "label": "Category Heading",
      "default": "Find the Perfect Perfume <em>for Everyone</em>"
    },
    {
      "type": "textarea",
      "id": "category_sub",
      "label": "Category Intro",
      "default": "Every great fragrance journey starts with the right choice. Pick a category and find your perfect scent."
    },
    {
      "type": "text",
      "id": "scent_eyebrow",
      "label": "Scent Eyebrow",
      "default": "Shop By Scent Family"
    },
    {
      "type": "html",
      "id": "scent_heading",
      "label": "Scent Heading",
      "default": "Find Your <em>Signature Note</em>"
    },
    {
      "type": "textarea",
      "id": "scent_sub",
      "label": "Scent Intro",
      "default": "Every signature fragrance starts with the right notes. Choose a scent family and find perfumes you'll love wearing every day."
    },
    {
      "type": "text",
      "id": "moment_eyebrow",
      "label": "Moment Eyebrow",
      "default": "Shop By Moment"
    },
    {
      "type": "html",
      "id": "moment_heading",
      "label": "Moment Heading",
      "default": "Find the Perfect Perfume for <em>Every Lifestyle & Climate</em>"
    },
    {
      "type": "textarea",
      "id": "moment_sub",
      "label": "Moment Intro",
      "default": "Shop premium perfumes for every lifestyle, occasion and climate, thoughtfully curated for every moment and every season."
    },
    {
      "type": "text",
      "id": "guides_eyebrow",
      "label": "Guides Eyebrow",
      "default": "Know Before You Buy"
    },
    {
      "type": "html",
      "id": "guides_heading",
      "label": "Guides Heading",
      "default": "Fragrance Insights & <em>Expert Guides</em>"
    },
    {
      "type": "textarea",
      "id": "guides_sub",
      "label": "Guides Intro",
      "default": "Get practical perfume guides, fragrance tips and expert advice to help you choose every scent confidently."
    },
    {
      "type": "text",
      "id": "hero_tagline",
      "label": "Hero tagline line 1",
      "default": "Find Your Signature Scent"
    },
    {
      "type": "text",
      "id": "hero_tagline_2",
      "label": "Hero tagline line 2",
      "info": "Rendered on its own line under the first. Leave blank for a one-line tagline.",
      "default": "at The Fragrance Secrets"
    }
  ],
  "blocks": [
    {
      "type": "@app"
    },
    {
      "type": "scent",
      "name": "Scent family",
      "settings": [
        {
          "type": "text",
          "id": "handle",
          "label": "Collection handle"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "info": "Defaults to the collection title."
        },
        {
          "type": "text",
          "id": "sub",
          "label": "Description"
        },
        {
          "type": "text",
          "id": "image_asset",
          "label": "Image (theme asset filename)",
          "info": "e.g. oud-woody-card.webp — the file must live in assets/. Takes precedence over the picker below."
        },
        {
          "type": "text",
          "id": "image_asset_sm",
          "label": "Small image (theme asset filename)",
          "info": "Optional 350 x 350 variant, served to phones."
        },
        {
          "type": "text",
          "id": "icon",
          "label": "Material icon name",
          "default": "spa"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Background image",
          "info": "Optional, 600 x 600 px. The card switches to light text over a dark scrim. Falls back to the collection image; without either it keeps the plain white card."
        }
      ]
    },
    {
      "type": "moment",
      "name": "Moment tile",
      "settings": [
        {
          "type": "select",
          "id": "column",
          "label": "Column",
          "options": [
            {
              "value": "lifestyle",
              "label": "By Lifestyle"
            },
            {
              "value": "climate",
              "label": "By Climate"
            }
          ],
          "default": "lifestyle"
        },
        {
          "type": "text",
          "id": "handle",
          "label": "Collection handle"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "info": "Defaults to the collection title."
        },
        {
          "type": "text",
          "id": "image_asset",
          "label": "Image (theme asset filename)",
          "info": "e.g. gym-tile.webp — 700 x 700, must live in assets/. Takes precedence over the picker."
        },
        {
          "type": "text",
          "id": "image_asset_sm",
          "label": "Small image (theme asset filename)",
          "info": "Optional 350 x 350 variant, served to phones."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Square. Used when no theme asset is named. Falls back to the collection image, then its first product photo."
        }
      ]
    },
    {
      "type": "category",
      "name": "Category card",
      "settings": [
        {
          "type": "text",
          "id": "handle",
          "label": "Collection handle"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "info": "Defaults to the collection title."
        },
        {
          "type": "text",
          "id": "sub",
          "label": "Descriptor",
          "info": "Shown under the title. Leave blank to show the live product count instead."
        },
        {
          "type": "text",
          "id": "image_asset",
          "label": "Image (theme asset filename)",
          "info": "e.g. mens-card.webp — the file must live in assets/. Takes precedence over the picker below."
        },
        {
          "type": "text",
          "id": "image_asset_sm",
          "label": "Small image (theme asset filename)",
          "info": "Optional 600 x 375 variant, served to phones."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "Landscape, 1200 × 750 px. Used when no theme asset is named above. Falls back to the collection image, then its first product photo."
        }
      ]
    },
    {
      "type": "why",
      "name": "Why choose card",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Material icon name",
          "default": "verified"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Heading"
        },
        {
          "type": "textarea",
          "id": "body",
          "label": "Body"
        }
      ]
    },
    {
      "type": "faq",
      "name": "FAQ",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Home",
      "blocks": [
        {
          "type": "category",
          "settings": {
            "handle": "mens",
            "title": "For Men",
            "image_asset": "mens-card.webp",
            "image_asset_sm": "mens-card-sm.webp"
          }
        },
        {
          "type": "category",
          "settings": {
            "handle": "womens",
            "title": "For Women",
            "image_asset": "womens-card.webp",
            "image_asset_sm": "womens-card-sm.webp"
          }
        },
        {
          "type": "category",
          "settings": {
            "handle": "unisex",
            "title": "Unisex",
            "image_asset": "unisex-card.webp",
            "image_asset_sm": "unisex-card-sm.webp"
          }
        },
        {
          "type": "category",
          "settings": {
            "handle": "kids",
            "title": "Kids Collection",
            "image_asset": "kids-card.webp",
            "image_asset_sm": "kids-card-sm.webp"
          }
        },
        {
          "type": "category",
          "settings": {
            "handle": "arabic",
            "title": "Arabic Perfumes",
            "sub": "Oud, musk & attar",
            "image_asset": "arabic-card.webp",
            "image_asset_sm": "arabic-card-sm.webp"
          }
        },
        {
          "type": "category",
          "settings": {
            "handle": "perfume-oil",
            "title": "Perfume Oils",
            "sub": "Alcohol-free & concentrated",
            "image_asset": "perfume-oil-card.webp",
            "image_asset_sm": "perfume-oil-card-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "office-wear",
            "image_asset": "office-wear-tile.webp",
            "image_asset_sm": "office-wear-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "date-night",
            "image_asset": "date-night-tile.webp",
            "image_asset_sm": "date-night-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "wedding",
            "image_asset": "wedding-tile.webp",
            "image_asset_sm": "wedding-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "gym",
            "image_asset": "gym-tile.webp",
            "image_asset_sm": "gym-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "travel-friendly",
            "image_asset": "travel-friendly-tile.webp",
            "image_asset_sm": "travel-friendly-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "lifestyle",
            "handle": "dinner-evening",
            "image_asset": "dinner-evening-tile.webp",
            "image_asset_sm": "dinner-evening-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "summer-wear",
            "image_asset": "summer-wear-tile.webp",
            "image_asset_sm": "summer-wear-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "winter-holiday",
            "image_asset": "winter-holiday-tile.webp",
            "image_asset_sm": "winter-holiday-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "desert-climate",
            "image_asset": "desert-climate-tile.webp",
            "image_asset_sm": "desert-climate-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "humid-weather",
            "image_asset": "humid-weather-tile.webp",
            "image_asset_sm": "humid-weather-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "tropical-climate",
            "image_asset": "tropical-climate-tile.webp",
            "image_asset_sm": "tropical-climate-tile-sm.webp"
          }
        },
        {
          "type": "moment",
          "settings": {
            "column": "climate",
            "handle": "rainy-day",
            "image_asset": "rainy-day-tile.webp",
            "image_asset_sm": "rainy-day-tile-sm.webp"
          }
        },
        {
          "type": "scent",
          "settings": {
            "handle": "oud-woody",
            "title": "Oud & Woody",
            "sub": "Deep, warm, resinous",
            "icon": "park",
            "image_asset": "oud-woody-card.webp",
            "image_asset_sm": "oud-woody-card-sm.webp"
          }
        },
        {
          "type": "scent",
          "settings": {
            "handle": "fresh-citrus",
            "title": "Fresh & Citrus",
            "sub": "Bright, clean, energetic",
            "icon": "water_drop",
            "image_asset": "fresh-citrus-card.webp",
            "image_asset_sm": "fresh-citrus-card-sm.webp"
          }
        },
        {
          "type": "scent",
          "settings": {
            "handle": "floral-rose",
            "title": "Floral & Rose",
            "sub": "Elegant, soft, romantic",
            "icon": "local_florist",
            "image_asset": "floral-rose-card.webp",
            "image_asset_sm": "floral-rose-card-sm.webp"
          }
        },
        {
          "type": "scent",
          "settings": {
            "handle": "sweet-gourmand",
            "title": "Sweet & Gourmand",
            "sub": "Rich, indulgent, cosy",
            "icon": "cake",
            "image_asset": "sweet-gourmand-card.webp",
            "image_asset_sm": "sweet-gourmand-card-sm.webp"
          }
        },
        {
          "type": "scent",
          "settings": {
            "handle": "spicy-oriental",
            "title": "Spicy & Oriental",
            "sub": "Bold, warm, exotic",
            "icon": "local_fire_department",
            "image_asset": "spicy-oriental-card.webp",
            "image_asset_sm": "spicy-oriental-card-sm.webp"
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "verified",
            "title": "100% Authentic Perfumes",
            "body": "Every fragrance is genuine and sourced through trusted suppliers, checked before it ships."
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "local_shipping",
            "title": "Fast UAE Delivery",
            "body": "Quick delivery across Dubai, Abu Dhabi, Sharjah and the wider Emirates."
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "lock",
            "title": "Secure Payments",
            "body": "Pay safely by card, wallet or cash on delivery, whichever suits you best."
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "diamond",
            "title": "Premium Fragrance Collection",
            "body": "Designer houses and Arabian perfumery, side by side in one place."
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "inventory_2",
            "title": "Carefully Packed Orders",
            "body": "Every order is packed with care so it arrives sealed, safe and gift-ready."
          }
        },
        {
          "type": "why",
          "settings": {
            "icon": "redeem",
            "title": "Perfect for Gifting",
            "body": "Ready-wrapped sets and premium packaging for weddings, Eid and birthdays."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "What are the best luxury perfumes to buy online in the UAE?",
            "answer": "The best luxury perfumes depend on your fragrance preferences, lifestyle and the occasion. Designer, Arabic and niche perfumes are popular for their quality, long-lasting performance and distinctive scent profiles. The Fragrance Secrets offers a carefully curated collection of authentic luxury perfumes from trusted global and regional brands."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "How do I choose the right perfume for my personality?",
            "answer": "Choosing the right perfume starts with understanding the fragrance notes you enjoy most. Fresh and citrus scents suit energetic personalities, floral fragrances offer timeless elegance, woody and oud perfumes create a bold impression, while oriental fragrances bring warmth and sophistication. The Fragrance Secrets helps you find the perfect scent for your personality and lifestyle."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "Where can I buy authentic perfumes online in Dubai?",
            "answer": "You can buy authentic perfumes online from trusted retailers that source products directly from reputable brands and distributors. The Fragrance Secrets offers a premium collection of designer, Arabic and luxury perfumes with delivery across the UAE, making online fragrance shopping simple, secure and reliable."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "Which perfumes last longer in UAE weather conditions?",
            "answer": "Perfumes with oud, amber, musk, woody and oriental fragrance notes generally perform well in the UAE's warm climate because they provide excellent longevity and projection. The Fragrance Secrets offers carefully selected perfumes designed to deliver lasting freshness and rich fragrance performance throughout the day."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "Are the perfumes sold at The Fragrance Secrets authentic?",
            "answer": "Yes. Every perfume available at The Fragrance Secrets is sourced from trusted suppliers and carefully verified for authenticity. Our collection includes genuine designer, Arabic and luxury fragrances, giving you complete confidence with every purchase."
          }
        },
        {
          "type": "faq",
          "settings": {
            "question": "Does The Fragrance Secrets deliver perfumes outside the UAE?",
            "answer": "Yes. The Fragrance Secrets delivers perfumes across the UAE and also offers international shipping to the USA and Canada. Customers can shop designer, Arabic and luxury perfumes online with reliable delivery and a seamless shopping experience."
          }
        }
      ]
    }
  ]
}
{% endschema %}
