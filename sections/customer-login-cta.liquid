{%- comment -%}
  Global band above the footer. Uses the same 10% membership band as the
  collection and hub pages, so there is one version of it across the site.
{%- endcomment -%}

{% render 'member-band' %}

{% schema %}
{
  "name": "Membership band",
  "settings": [
    { "type": "paragraph", "content": "Shows the 10% welcome-discount band above the footer, hidden for signed-in customers. The copy lives in snippets/member-band.liquid so every page stays identical." }
  ],
  "presets": [ { "name": "Membership band" } ]
}
{% endschema %}
