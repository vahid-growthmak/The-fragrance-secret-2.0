# Secrets — never commit
.env
.env.local
*.token

# Python byproducts from scripts/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/

# Store backups — contain customer, order and pricing data. Never commit.
live-backups/
store-backups/
